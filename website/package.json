{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "pnpm run docs:generate && vitepress build",
    "preview": "vitepress preview",
    "docs:generate": "typedoc --options typedoc.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linting configured'"
  },
  "devDependencies": {
    "typedoc": "^0.28.17",
    "typedoc-plugin-markdown": "^4.10.0",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4"
  }
}
