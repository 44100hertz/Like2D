### Online IDE!!

There are libraries and websites for exactly this.
"Try LIKE in your browser".
It might not be much work for this, and it would be a big win.

### Interactive tutorial with code examples

Investigate this, how hard would it be?

### Documentation pain points to address (v1.8.1)

Based on v2.8.0 planning, these concepts need clear explanation:

- [x] **Scene lifecycle** - Documented in `scene.ts`, event order in `events.ts`
- [ ] **Focus/blur behavior** - What triggers these, what the game should do in response
- [ ] **Input mapping** - How to bind actions, why use it over direct input checks
- [ ] **Pointer lock** - When it activates, how to control it, mouse visibility modes
- [ ] **Scaling modes** - `setMode` options, pixel-perfect vs smooth scaling, fullscreen behavior

### IDE autocomplete priorities

Users want tooltips on:
- [x] `Scene` interface methods (load, update, draw, event handlers)
- [x] `Like` interface properties (gfx, audio, input, timer, etc.)
- [ ] `setMode` parameters
- [ ] Input mapping API (`map`, `unmap`, `isDown`, `justPressed`)
- [ ] Graphics methods (`draw`, `clear`, `newImage`)

### Example website structure

```
website/
├── guides/
│   ├── getting-started.md
│   ├── callback-pattern.md
│   ├── scene-pattern.md
│   ├── input-mapping.md
│   ├── scaling-modes.md
│   └── migrating-from-love2d.md
├── api/          # Generated by TypeDoc
│   ├── graphics/
│   ├── audio/
│   ├── input/
│   └── ...
├── philosophy.md  # Links to docs/PHILOSOPHY.md
└── index.md       # Landing page
```

### Video tutorials?

---
### Pain Point Documentation Status

| Pain Point | Status | Location |
|------------|--------|----------|
| Scene lifecycle | Complete | `scene.ts`, `events.ts` |
| Event binding scope | Not started | `core/keyboard.ts`, `core/mouse.ts` |
| Focus/blur behavior | Complete | `engine.ts` |
| Input mapping | Not started | `core/input.ts` |
| preventDefault strategy | Not started | `core/keyboard.ts` |
| Pointer lock | Partial | `core/mouse.ts` |
| Scaling modes | Not started | `core/canvas-manager.ts` |

### Recommendations

1. **Next up: Priority 2** - Graphics API is the biggest user-facing gap
2. **Link to EventMap** - All callback-related docs should reference it
3. **Follow cozy tone** - Practical examples over philosophy
4. **Use @see links** - Avoid duplicating callback documentation
5. **Keep it minimal** - Per PHILOSOPHY VII: "Say more in fewer words"

### Known Issues / Testing Needed

**Testing required:** Verify in VS Code, WebStorm, and Vim/Neovim LSP whether:
1. Callback descriptions appear when hovering `game.update`
2. Parameters show types and descriptions
3. Links to `EventMap` are clickable

**Potential fixes if needed:**
- Add explicit property declarations for common callbacks (load, update, draw, keypressed)
- Duplicate key descriptions in `LikeWithCallbacks` interface docs
- Use TypeDoc's `@inheritDoc` if supported

